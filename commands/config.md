---
allowed-tools: Bash
description: Open notification settings in your editor
---

Open the Claude Code Notify config file. Creates default config if it doesn't exist.

```bash
"${CLAUDE_PLUGIN_ROOT}/scripts/open-config.sh"
```
