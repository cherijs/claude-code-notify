---
allowed-tools: Bash
description: Show notification status (session and global)
---

Show current notification status for both session and global settings.

```bash
"${CLAUDE_PLUGIN_ROOT}/scripts/notification-sound.sh" --status
```
